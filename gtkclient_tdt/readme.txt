# GtkClient (TDT)

This is a branch of "GtkClient" that works with with TDT recording hardware.

## Building

First get the dependencies by typing `make deps`. This should work on Debian and might work on Ubuntu. Next build by typing `make`.
